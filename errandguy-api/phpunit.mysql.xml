<?xml version="1.0" encoding="UTF-8"?>
<!--
    MySQL test config — runs the suite against a REAL MySQL engine so the
    money-safety row locks (lockForUpdate) and unique-index backstops are
    actually exercised, unlike the default SQLite config where compileLock is a
    no-op (audit QA-1 / DEVOPS-7).

    SAFETY: the DB connection below is hardcoded to 127.0.0.1 / a throwaway
    `errandguy_ci_test` database with force="true", so it can NEVER resolve to
    the Forge production host even if a local .env points there. Create the DB
    with: mysql -h127.0.0.1 -uroot -e "CREATE DATABASE errandguy_ci_test".
    Run with: vendor/bin/phpunit -c phpunit.mysql.xml
-->
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <!-- The suite renders real PDFs (dompdf/Cpdf), so accumulated memory
             crosses the PHP CLI default of 128M and fatally OOMs mid-run. Pin a
             comfortable ceiling so the suite (and CI) stays deterministic. -->
        <ini name="memory_limit" value="512M"/>
        <env name="APP_ENV" value="testing" force="true"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file" force="true"/>
        <env name="BCRYPT_ROUNDS" value="4" force="true"/>
        <env name="BROADCAST_CONNECTION" value="null" force="true"/>
        <env name="CACHE_STORE" value="array" force="true"/>
        <env name="DB_CONNECTION" value="mysql" force="true"/>
        <env name="DB_HOST" value="127.0.0.1" force="true"/>
        <env name="DB_PORT" value="3306" force="true"/>
        <env name="DB_DATABASE" value="errandguy_ci_test" force="true"/>
        <env name="DB_USERNAME" value="root" force="true"/>
        <env name="DB_PASSWORD" value="" force="true"/>
        <env name="DB_URL" value="" force="true"/>
        <env name="MAIL_MAILER" value="array" force="true"/>
        <env name="QUEUE_CONNECTION" value="sync" force="true"/>
        <env name="SESSION_DRIVER" value="array" force="true"/>
        <env name="PULSE_ENABLED" value="false" force="true"/>
        <env name="TELESCOPE_ENABLED" value="false" force="true"/>
        <env name="NIGHTWATCH_ENABLED" value="false" force="true"/>
    </php>
</phpunit>
